web: infisical run -- bin/rails server -p $PORT
css: yarn build:css --watch
js: yarn build --reload

# docker: docker-compose upstripe: stripe listen --forward-to localhost:3000/webhooks/stripe
worker: infisical run -- bundle exec sidekiq
