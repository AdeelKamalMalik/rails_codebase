<div class="h-screen w-full items-center bg-gradient-to-br from-primary-50 via-teal-100 to-purple-100">
  <div class="flex items-center justify-center h-screen w-full">
    <div class="border w-full min-h-[316px] md:w-2/3 lg:w-1/3 bg-white p-5 mx-5 rounded-lg">
      <div class="mb-5 font-semibold pt-3">Select verification method</div>
      <div class="mb-3 font-normal bg-gray-50 p-3">
        <div class="flex items-center justify-between gap-x-6">
          <%= image_tag "icons/scan.svg", class: 'self-baseline hidden md:block' %>
          <div class="lg:w-2/3">
            <p class="font-semibold">Use Authenticator App</p>
            <p class="text-grey-500">Scan QR code with camera and use an authenticator app of your choice</p>
          </div>
          <%= link_to "Use App", backup_codes_user_two_factor_path, class: "flex-shrink-0 btn btn-white" %>
        </div>
      </div>
      <div class="mb-3 mt-6 font-normal bg-gray-50 p-3">
        <div class="flex items-center justify-between gap-x-6">
          <%= image_tag "icons/send-email.svg", class: 'self-baseline hidden md:block' %>
          <div class="lg:w-2/3">
            <p class="font-semibold">Use Phone Number</p>
            <p class="text-grey-500">Receive sign-in codes on a phone number</p>
          </div>
          <%= link_to "Send SMS", add_phone_user_two_factor_path, class: "flex-shrink-0 btn btn-white" %>
        </div>
      </div>
      <%= link_to "Remind me later", root_path, class: "btn btn-white float-right mt-3" %>
    </div>
  </div>
</div>
