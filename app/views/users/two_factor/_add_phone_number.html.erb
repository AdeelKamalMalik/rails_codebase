<div class="relative z-10 hidden" id="<%= modal_id %>" aria-labelledby="modal-title" role="dialog" aria-modal="true" data-controller="settings">
  <div class="fixed inset-0 bg-gray-100 bg-opacity-75 transition-opacity"></div>
  <div class="fixed inset-0 z-10 w-screen overflow-y-auto border">
    <div class="flex min-h-full justify-center p-4 text-center items-center sm:p-0">
      <div class="relative transform overflow-hidden rounded-lg bg-white px-4 pb-4 pt-5 text-left  transition-all sm:my-8 sm:w-full sm:p-6 lg:w-1/3 border">
        <div>
          <h6 class="font-semibold" id="modal-title">Add a phone number</h6>
        </div>

        <div class="relative">
          <% 
            country_names_with_codes = ISO3166::Country.all.map{ |x| ["#{x.alpha2} (#{x.country_code})", x.alpha2]}
          %>
          <%= select_tag :country_code, options_for_select(country_names_with_codes), class: "absolute border-none bottom-0 h-11", id: "add_phone_country_code", data: {'settings-target': 'countryCode'} %>
          <input type="tel" id="phoneNumber" data-settings-target="phoneNumber" class="mt-5 h-12 placeholder-[#9CA3AF] rounded-lg border-gray-200 w-full pl-[105px] text-[#9CA3AF]" placeholder="(255) 567-4455">
        </div>
        <p class="text-sm mt-1 text-red-600" id="verify-phone-error"></p>
        <div class="mt-5 text-sm text-gray-500">
          <p class="text-base">Contractful will use this number to help you sign in and to alert you if there's unusual activity in your account.</p>
          <ul class="text-base">
            <li class="mt-3">Don't use a Google Voice number.</li>
            <li class="mt-3">Charges from your carrier may apply.</li>
          </ul>
        </div>
       
        <div class="flex flex-col md:flex-row justify-between items-center mt-20">
          <div class="flex items-center justify-center gap-4">
            <%= link_to 'About Privacy Policy','https://contractful.co/terms/privacy', target: "_blank", class: "text-blue-700 cursor-pointer mb-3 font-semibold" %>
            <%= image_tag 'icons/question.svg', class: "mb-2.5" %>
          </div>
          <div class="flex justify-between">
            <p class="hover:text-blue-500 hover:bg-white btn btn-white border-blue-700 text-blue-700" data-action="click->settings#closeModal" data-modal-id="addPhoneNumber"><%= t("users.two_factor.change_auth_app.cancel") %></p>
            <%= render "shared/button", button_text: "Continue", disable_btn_text: 'Processing...', data_action: 'click->settings#addPhone' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
