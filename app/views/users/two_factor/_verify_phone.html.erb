<div class="relative z-10 hidden" id="<%= modal_id %>" aria-labelledby="modal-title" role="dialog" aria-modal="true" data-controller="settings">
  <div class="fixed inset-0 bg-gray-100 bg-opacity-75 transition-opacity"></div>
  <div class="fixed inset-0 z-10 w-screen overflow-y-auto border">
    <div class="flex min-h-full justify-center p-4 text-center items-center sm:p-0">
      <div class="relative transform overflow-hidden rounded-lg bg-white px-4 pb-4 pt-5 text-left  transition-all sm:my-8 sm:w-full sm:p-6 lg:w-1/3 border">
        <div>
          <h6 class="font-semibold" id="modal-title">Verify this phone number</h6>
        </div>

        <input type="text" id="phone_verification" class="mt-5 h-12 rounded-lg border-gray-200 w-full text-[#9CA3AF]" placeholder="Enter verification code" data-settings-target="phoneNumber">
        <p class="text-sm mt-1 text-red-600" id="m-verify-phone-error"></p>
        <div class="mt-5 text-sm text-gray-500">
          <p class="text-base">When you add a number for 2-Step Verification, Contractful will send you a text message to make sure the number is yours.</p>
          <ul class="text-base">
            <li class="mt-3">Contractful will use this number to help you sign in and to alert you if there's unusual activity in your account.</li>
            <li class="mt-3">Don't use a Google Voice number.</li>
            <li class="mt-3">Charges from your carrier may apply.</li>
          </ul>
        </div>
       
        <div class="flex flex-col md:flex-row justify-between items-center mt-20">
          <div class="flex items-center justify-center gap-4">
            <%= link_to 'About Privacy Policy','https://contractful.co/terms/privacy', target: "_blank", class: "text-blue-700 cursor-pointer mb-3 font-semibold" %>
            <%= image_tag 'icons/question.svg', class: "mb-2.5" %>
          </div>
          <div class="flex justify-between">
            <p class="hover:text-blue-500 hover:bg-white btn btn-white border-blue-700 text-blue-700" data-action="click->settings#closeModal" data-modal-id="<%= modal_id %>"><%= t("users.two_factor.change_auth_app.cancel") %></p>
            <%= render "shared/button", button_text: "Verify", disable_btn_text: 'Verifying...', data_action: 'click->settings#verifyPhoneNo' %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
